{
  "name": "deploy-agent",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile --minify src/index.ts --outfile deploy-agent",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.48",
    "elysia": "^1.0.0",
    "mongodb": "^6.21.0",
    "pg": "^8.22.0",
    "redis": "^6.2.0"
  },
  "devDependencies": {
    "@types/node": "^25.1.0",
    "@types/pg": "^8.20.3",
    "bun-types": "latest",
    "typescript": "^5.0.0"
  }
}
